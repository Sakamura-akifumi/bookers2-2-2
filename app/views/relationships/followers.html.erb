<div class='container'>
  <div class='row'>
    <div class='mx-auto'>
      <h2>Follower Users</h2>
      　<% if @users.exists? %>
          <%= render '/users/index', users: @users %>
        <% else %>
          <center>フォローされているユーザーはいません</center>
        <% end %>
    </div>
  </div>
</div>