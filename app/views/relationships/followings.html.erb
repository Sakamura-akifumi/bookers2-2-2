<div class='container'>
  <div class='row'>
    <div class='mx-auto'>
      <h2>Follow Users</h2>
      <% if @users.exists? %>
        <%= render '/users/index', users: @users %>
      <% else %>
        <p>フォローしているユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>